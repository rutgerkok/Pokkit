machine:
  java:
version: oraclejdk8

checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean
    - mvn package
    - cp -R ./target/Pokkit*.jar $CIRCLE_ARTIFACTS
